# Timeline preview

Interactive viewer for CLIPSeg study outputs: hover over the **district vibe timeline** (left) to see the matching **per-image report** (right).

## Requirements

- Python 3.10+
- See `requirements.txt`

```bash
pip install -r requirements.txt
